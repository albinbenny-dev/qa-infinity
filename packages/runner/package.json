{
  "name": "@qa-infinity/runner",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright test runner sandbox — invoked by qa-api via Docker exec",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "@playwright/test": "^1.45.0"
  }
}
