// playwright.config.ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  use: {
    // Treat the standard 'id' attribute as the test id
    testIdAttribute: 'id', 
  },
});