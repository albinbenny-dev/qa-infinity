{
  "name": "qa-infinity",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous AI-powered QA automation platform",
  "scripts": {
    "dev": "concurrently -n api,ui -c cyan,magenta \"pnpm --filter @qa-infinity/api dev\" \"pnpm --filter @qa-infinity/frontend dev\"",
    "build": "pnpm --filter @qa-infinity/api build && pnpm --filter @qa-infinity/frontend build",
    "type-check": "pnpm --filter @qa-infinity/api tsc --noEmit && pnpm --filter @qa-infinity/frontend type-check",
    "docker:up": "docker compose up --build",
    "docker:up:detach": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:logs:api": "docker compose logs -f qa-api",
    "docker:reset": "docker compose down -v && docker compose up --build -d"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "packageManager": "pnpm@9.15.9",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^9.0.0",
      "onFail": "error"
    }
  }
}
